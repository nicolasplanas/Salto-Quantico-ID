{
  "buildCommand": "cd backend && pip install -r requirements.txt --no-cache-dir && cd ../frontend && npm install && npm run build",
  "outputDirectory": "frontend/dist",
  "env": {
    "PYTHON_VERSION": "3.11"
  },
  "functions": {
    "backend/app.py": {
      "runtime": "python3.11"
    }
  }
}
